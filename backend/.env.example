# Server
PORT=3000

# Postgres connection (adjust username, password, host, db)
DATABASE_URL=postgres://user:password@localhost:5432/workzen

# Auth / Security
JWT_SECRET=changeme_dev_secret
BCRYPT_ROUNDS=10

# SMTP (optional for forgot password)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=username
SMTP_PASS=password
SMTP_FROM=no-reply@workzen.local
# For hackathon demos you can direct all reset mails to a team inbox
RESET_TEST_EMAIL=team@workzen.local
